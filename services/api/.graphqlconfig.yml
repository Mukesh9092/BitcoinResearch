projects:
  app:
    schemaPath: src/datamodel.graphql
    extensions:
      endpoints:
        # TODO: Don't cardcode port here. Take from .env file.
        default: api.docker.localhost
  prisma:
    schemaPath: src/datamodel.prisma.gen.graphql
    extensions:
      prisma: prisma.yml
