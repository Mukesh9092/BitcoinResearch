# This must be localhost, because prisma deploy is ran from the host machine.
endpoint: http://prisma.docker.localhost
datamodel: src/datamodel.graphql

generate:
  - generator: graphql-schema
    output: ../../common/graphql/generated/prisma/schema.graphql
  - generator: javascript-client
    output: ../../common/graphql/generated/prisma/client/
