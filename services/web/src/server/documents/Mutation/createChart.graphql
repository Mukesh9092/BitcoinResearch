mutation createChart(
  $dashboardId: ID!
  $marketId: ID!
  $period: Period!
  $from: DateTime!
  $to: DateTime!
) {
  createChart(
    dashboardId: $dashboardId
    marketId: $marketId
    period: $period
    from: $from
    to: $to
  ) {
    ...Chart
  }
}
