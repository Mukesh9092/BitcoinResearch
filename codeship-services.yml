app:
  build:
    dockerfile: Dockerfile-app
    image: eu.gcr.io/test-next.js/app
  cached: true
  ports:
    - "3000:3000"
  # links:
  #   - backend
  # environment:
  #   - BACKEND_URL=http://backend:8089
