#!/usr/bin/env bash

nodemon \
    -e js,jsx \
    -V \
    --delay 2500ms \
    --watch ./services/${SERVICE_NAME} \
    --ignore **/*.json \
    --ignore node_modules \
    --exec 'npm run start-inspect'
